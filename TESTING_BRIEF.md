# VerseCraft 多 Agent 边界情况真实试玩测试

## 零、开始前确认
- 默认跑在本地 Postgres / Redis / pgvector，不是生产环境
- 转职、战斗目前有没有锁定的 `tests/`/`SPEC.md`？没有的话阶段 A 末尾有一个人工确认点（见 2.3）；有的话可以整段自动跑完，不用人工卡点
- 如果这轮也要覆盖原石叙事扣除系统，在 2.2 补一节，规则一致：先列系统专属边界，再补进"系统交叉边界"
- 并行 agent 数量、轮次上限跟 Qwen 预算挂钩，这两个数字写在 `/goal` 调用那一行，不要写死在这份文件里

## 一、目标
用真实 LLM 驱动（禁止 mock/stub）的多个"玩家" agent，对武器、职业/转职、任务、战斗四大系统（含彼此交叉场景）做长程真实试玩，每条路径都要走到通关或角色死亡，系统性触发边界情况；发现的问题进入既有修复循环（锁定 tests/、只改实现代码、先写会失败的回归测试再修复），全程不得放宽验收标准。

## 二、阶段 A：多 Agent 真实试玩（发现问题）

### 2.1 试玩方式
开 [3–5，按预算调整] 个独立玩家 agent，每个给不同游玩倾向以提高覆盖而非互相重复：稳健通关型 / 边界试探型（专挑反直觉与极限数值）/ 速通型 / 完全体验型（走满支线、囤积或耗尽资源）。每个独立跑到**通关或死亡**（两种都要有 agent 覆盖），单条路径设安全上限（最多 [200，可调] 个游戏内动作），防止卡死不收敛。铺量试玩可用性价比模型，一旦某条路径疑似触发问题需要根因分析，再切换到更强模型。

再单独跑一组**并发压力试玩**：多个 agent 对同一角色/同一状态同时发起操作（如一个正在转职、另一个同时把它拉进战斗），专门验证 Postgres 事务和 Redis 锁在并发写入下是否真的生效——这是找竞态 bug，跟"多开局提高覆盖率"目的不同，两个都要做。

### 2.2 边界情况清单（每类至少真实触发验证一次，缺项要在报告里写明原因）

**武器**：耐久归零/负值；不满足职业/等级限制仍装备；强化到系统上限及超出；背包溢出/重复叠加；互斥武器同时装备；战斗中损坏后的状态转换

**职业 / 转职**（转职本质是职业系统的状态转换动作，边界 bug 通常集中在转换瞬间）：战斗中触发转职；转职后技能/装备继承是否正确（含旧装备不再满足新职业条件时的处理）；转职条件边界值（刚好满足/差一点不满足）；短时间连续转职；转职到不存在/已下架的职业；同一角色被并发发起不同转职请求

**任务**：状态机非法跳转（前置未完成却触发后续）；失败/超时/主动放弃分支；并行任务间资源冲突；奖励发放幂等性（重复触发是否重复发奖）；进行中任务的角色死亡后状态如何处理

**战斗**（大部分跨系统 bug 会在这里暴露）：HP/资源归零、负值、buff 叠加溢出；极端数值输入（0 伤害、超高伤害、除零）；LLM 调用超时/异常返回时战斗状态如何恢复；多角色同时行动的顺序与并发一致性；死亡结算与复活/重开；战斗内触发的转职/任务完成/掉落等跨系统事件顺序是否正确

**系统交叉边界（优先级最高）**：战斗中死亡 + 转职进行中；战斗中死亡 + 任务奖励结算中；转职后原武器与新职业不兼容；一个 agent 转职同时另一个 agent 让同一角色进入战斗

**LLM 叙事一致性**（常规 QA 测不到，是你们产品特有的雷区）：角色已死/HP=0 后叙事是否还描述其行动；武器已损坏/卸下后叙事是否还描述其生效；玩家输入越界指令（不存在的道具、绕过转职前提）时 LLM 是否顺着编、生成破坏规则的内容；长上下文/多轮后是否还准确记得关键状态。这类问题不好用传统断言测，直接复用黄金测试集 + LLM-as-Judge：试玩里凡出现状态和叙事对不上的情况，整理成新的黄金用例，评分低于阈值算不过

**基础设施**：多 agent 并发写入同一状态的一致性（Postgres 事务/Redis 锁是否真的生效）；pgvector 检索在长期记忆场景下的正确性；存档/读档后各系统状态（武器耐久、职业与技能、任务进度、战斗内计时器）是否都保持一致，不要只验证单一系统；上下文长度触及模型上限时的截断处理

### 2.3 阶段 A 产出
每个发现的问题写进 `PLAYTEST_FINDINGS.md`：触发它的 agent/路径、复现步骤、期望行为 vs 实际行为、初步严重程度。

职业/武器/任务（含原石叙事扣除，若这轮覆盖）若已有锁定的 `tests/`+`SPEC.md`，发现的问题直接进阶段 B。转职、战斗若目前没有对应锁定测试，除写进 `PLAYTEST_FINDINGS.md`，也顺手起草一条能复现问题的新测试放进 `tests/`，标记为 draft——不要自己锁定自己写的测试。

**人工确认点**：若阶段 A 产出了 draft 测试，醒来后花几分钟扫一眼是否写得对、要不要调整，去掉 draft 标记后再进阶段 B；若这轮转职/战斗已有锁定基线（没有 draft 产出），两阶段可以在同一个 `/goal` 里连着跑。

## 三、阶段 B：修复循环（沿用现有机制，不重新发明）
1. 跑 `date '+%F %T'`，时间戳写入 `LOG.md` 本轮开头
2. 读 `LOG.md`：接上一轮进度；同一问题连续 3 轮没修好 → 跳到第 6 步 escalate
3. 完整跑一遍验收测试，把测试运行器的【原始输出】（通过/失败数、退出码、报错栈）原样贴出，不许只写"通过了"代替原始输出
4. 挑一个红的问题：先写清根因（实现 bug 还是 spec 没覆盖），只改【实现代码】让它变绿；不许碰 `tests/` 与 `SPEC.md`
5. git commit，message 写清本轮改了什么、为什么；追加一行到 `LOG.md`：轮次/时间戳/修的问题/改了哪些文件/当前红绿数
6. 判定并输出结束信号：全部通过且无测试被削弱 → 打印 `ALL_GREEN`；同一问题连续 3 轮修不好 → 根因写进 `ISSUES.md`，打印 `ESCALATED`；否则不打印，进入下一轮

## 四、禁止事项
- 不许通过删除/注释测试用例、放宽断言阈值、加 try/catch 吞异常、打 skip 标记等方式让测试"变绿"
- 阶段 A 的试玩 agent 不许为跑得快而回避边界情况——清单每一类都要有真实触发的动作日志，不能只叙述"已覆盖"
- 某个边界情况确实这轮修不了，如实记录进 `ISSUES.md` 并说明原因，不能悄悄跳过或弱化验证

## 五、验收标准
- 2.2 清单每一类都有真实触发+验证记录
- 至少 [5–10，可调] 条独立完整试玩路径，覆盖不同职业组合、至少一条完整通关、至少两种不同死法
- 并发压力试玩覆盖"系统交叉边界"里列的每一种组合
- 所有已修复问题都有对应回归测试且通过
- 无未说明原因的遗留问题

## 六、交付物
`TEST_REPORT_<日期>.md`：汇总 `PLAYTEST_FINDINGS.md`/`ISSUES.md`/`LOG.md`，包含清单覆盖情况、问题列表（严重程度/复现步骤/根因/修复方式/回归测试）、未解决问题及原因、结论（明确给出是否达标，不要模糊带过）
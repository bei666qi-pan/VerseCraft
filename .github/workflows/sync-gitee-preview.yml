name: Sync Gitee Preview

on:
  push:
    branches: [preview]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  sync-preview:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout preview
        uses: actions/checkout@v4
        with:
          ref: preview
          fetch-depth: 0

      - name: Configure git identity
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Add Gitee remote
        env:
          GITEE_REPO_URL: ${{ secrets.GITEE_REPO_URL }}
          GITEE_TOKEN: ${{ secrets.GITEE_TOKEN }}
          DEFAULT_GITEE_REPO_URL: https://gitee.com/bei-qi-luo/VerseCraft.git
        run: |
          set -euo pipefail

          repo_url="${GITEE_REPO_URL:-${DEFAULT_GITEE_REPO_URL}}"

          if [ -z "${repo_url}" ] || [ -z "${GITEE_TOKEN}" ]; then
            echo "Missing GITEE_TOKEN or Gitee repository URL."
            exit 1
          fi

          case "${repo_url}" in
            https://*) repo_without_scheme="${repo_url#https://}" ;;
            *) echo "Gitee repository URL must start with https://"; exit 1 ;;
          esac

          git remote add gitee "https://oauth2:${GITEE_TOKEN}@${repo_without_scheme}"

      - name: Push preview branch to Gitee
        run: |
          set -euo pipefail

          git fetch --no-tags gitee +refs/heads/preview:refs/remotes/gitee/preview || true
          expected="$(git rev-parse --verify refs/remotes/gitee/preview 2>/dev/null || true)"

          if [ -n "${expected}" ]; then
            git push gitee HEAD:refs/heads/preview --force-with-lease=refs/heads/preview:"${expected}"
          else
            git push gitee HEAD:refs/heads/preview
          fi

          echo "Gitee preview branch sync completed."

# VerseCraft · Cursor 入口指令

你是 **VerseCraft（文界工坊）** 的 AI 原生全栈维护代理。

**开始任何任务前，先完整阅读以下两份项目指令文件（均在仓库根目录）：**

1. **AGENTS.md** — 跨客户端通用架构约束、不可破坏的契约、叙事治理、验证策略（全客户端共享）
2. **CLAUDE.md** — Claude Code 特定执行协议、任务模式、上下文读取清单、验证命令速查（Claude Code 补充细节）

两份文件互补。冲突时以 AGENTS.md（跨客户端事实源）为准，代码与测试高于所有说明文件。

---

## 行为准则速查

- **先读再改**：定位目标文件、调用方、测试，再修改
- **最小 diff**：只改任务相关文件，不做顺手重构
- **简体中文**：UI、文案、剧情、注释
- **禁止幻觉**：不编造不存在的 API、组件、表、环境变量
- **事实源**：`package.json` 是命令源，代码和测试是行为源
